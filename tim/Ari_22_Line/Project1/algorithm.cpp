/*	vector <myPoint> points;
	vector <myLine> lines;
	myLine recentLine;
	int sizeOfPoints=points.size();
	for(int i=0;i<sizeOfPoints;i++){
		recentLine=lines[i];
		
			if（点在已知直线上）
				删除点
			else
				遍历剩余点
					if（当前点和剩余点组成直线有效）
						添加直线，删除点
		
		
	}*/